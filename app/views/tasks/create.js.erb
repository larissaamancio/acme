<% if @task.errors.any? %>
  $("#notes_modal_campaign").html(modal);
<% else %>
  var modal = '<%= j render("lists/accordion_list", {:note => Task.new})%>';

	$("#input_tasks").val("");
	$(".modal-body").append(modal);	
<% end %>